# Logo institucional

Poné acá el archivo `logo-alvarado.png` (el escudo del colegio).
Lo usa el endpoint `GET /votes/acta` para imprimir el header del PDF.

Si el archivo no está, el acta se genera igual pero sin logo.
